rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    function isAdmin() {
      return request.auth != null && (
        request.auth.token.email == 'shahariar.sabbir.101@gmail.com' ||
        request.auth.token.email == 'shahariar.sabbir.01@gmail.com'
      );
    }

    match /projects/{document} {
      allow read: if true;
      allow write, update, delete: if isAdmin();
    }
    
    match /career/{document} {
      allow read: if true;
      allow write, update, delete: if isAdmin();
    }
    
    match /config/{document} {
      allow read: if true;
      allow write, update, delete: if isAdmin();
    }

    match /skills/{document} {
      allow read: if true;
      allow write, update, delete: if isAdmin();
    }

    match /testimonials/{document} {
      allow read: if true;
      allow write, update, delete: if isAdmin();
    }
    
    match /blogPosts/{document} {
      allow read: if true;
      allow write, update, delete: if isAdmin();
    }
    
    match /journalEntries/{document} {
      allow read: if true;
      allow write, update, delete: if isAdmin();
    }

    match /certifications/{document} {
      allow read: if true;
      allow write, update, delete: if isAdmin();
    }
    
    match /contactMessages/{document} {
      allow create: if request.resource.data.keys().hasAll(['name', 'email', 'message', 'createdAt', 'read'])
                    && request.resource.data.keys().hasOnly(['name', 'email', 'message', 'createdAt', 'read'])
                    && request.resource.data.name is string
                    && request.resource.data.name.size() < 100
                    && request.resource.data.email is string
                    && request.resource.data.email.size() < 150
                    && request.resource.data.message is string
                    && request.resource.data.message.size() < 2000;
      allow read, update, delete: if isAdmin();
    }
  }
}
